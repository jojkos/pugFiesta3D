# Frontend (safe to expose, used by the browser)
VITE_SUPABASE_URL=https://xlwwkcpvnrpdptzgsxsu.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=

# Optional: override the TTS proxy URL. Defaults to /api/tts on the same origin.
# VITE_TTS_PROXY_URL=https://your-app.vercel.app/api/tts

# --- Server-only (set in Vercel project env vars, NOT here in committed dotenv) ---
# ELEVENLABS_API_KEY=
